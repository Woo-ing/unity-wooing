import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  base: '/unity-wooing/', // 设置部署到 GitHub Pages 的子路径
})
